# CDR-User-Interface-Testing
Module used in Arduino IDE to connect Bluetooth module to a moblie app.

By running this code in Arduino IDE, the Arduino becomes available to find on a BLE connection app. Using LightBlue, the name UNO R4 can be connected to and have values read/written to it instantaneously from up to 50 meters away.
